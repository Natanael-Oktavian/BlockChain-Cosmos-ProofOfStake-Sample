syntax = "proto3";
package natancoin.natancoin.v1;

import "amino/amino.proto";
import "gogoproto/gogo.proto";
import "natancoin/natancoin/v1/params.proto";

option go_package = "natancoin/x/natancoin/types";

// GenesisState defines the natancoin module's genesis state.
message GenesisState {
  // params defines all the parameters of the module.
  Params params = 1 [
    (gogoproto.nullable) = false,
    (amino.dont_omitempty) = true
  ];
}
